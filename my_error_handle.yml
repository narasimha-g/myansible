--- 
 - hosts: web_servers
   become: yes
   gather_facts: yes 
   tasks:
   - name: i want to exicute linux commands
     command: hostname -f 
     register: myresult

   - name: i want to display above task 
     dibug: 
      msg: "{{myresult}}"
   